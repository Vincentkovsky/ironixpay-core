[package]
name = "migration"
version = "0.1.0"
edition = "2021"

[dependencies]
sea-orm-migration = { version = "1.0", features = [
  "sqlx-postgres",
  "runtime-tokio-rustls",
] }
tokio = { version = "1", features = ["full"] }
